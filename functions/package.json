{
  "name": "rescatto-functions",
  "scripts": {
    "lint": "node -e \"console.log('lint skipped - JS functions')\"",
    "build": "node -e \"console.log('build skipped - JS functions')\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "@sendgrid/mail": "^8.1.6",
    "crypto-js": "^4.2.0",
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^7.3.0",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true
}
